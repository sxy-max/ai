[project]
name = "go-ai-agent-runtime"
version = "0.1.0"
requires-python = ">=3.11,<3.15"
dependencies = [
  "agentscope[service,storage-redis,workspace-docker,tools]==2.0.6",
]

[project.optional-dependencies]
test = ["pytest==8.4.2"]

[tool.pytest.ini_options]
testpaths = ["tests"]

